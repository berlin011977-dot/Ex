{
  "name": "exam-platform-public",
  "version": "1.0.0",
  "description": "Public exam platform with owner dashboard, shared storage, and a student exam flow.",
  "private": true,
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "tunnel": "start-public-url.cmd",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "dependencies": {
    "@libsql/client": "^0.17.2",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "iconv-lite": "^0.4.24",
    "mammoth": "^1.9.1",
    "multer": "^2.0.0",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
